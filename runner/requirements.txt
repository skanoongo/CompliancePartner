# Pinned: a capture run is evidence, so the toolchain that produced it should be
# reproducible from the commit alone.
playwright==1.48.0
openpyxl==3.1.5
pillow==11.0.0
mss==9.0.2
Flask==3.0.3
waitress==3.0.1
PyYAML==6.0.2
